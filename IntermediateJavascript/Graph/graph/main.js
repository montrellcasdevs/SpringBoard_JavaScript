import { Graph } from "./graph";
import { Node } from "./graph";

const graph = new Graph();
// Add nodes and edges to the graph
graph.addNode("Albert");
graph.addNode("Brian");
graph.addEdge("Albert", "Brian");
